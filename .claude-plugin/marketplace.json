{
  "name": "swarmcloud",
  "owner": {
    "name": "SwarmCloud"
  },
  "metadata": {
    "description": "SwarmCloud's own tooling, for operators and agents working in this repository."
  },
  "plugins": [
    {
      "name": "sc",
      "source": "./plugin",
      "description": "SwarmCloud from inside a Claude Code session: read the cluster with the `sc` skill and /sc command, run this session's work out there with the `delegate` skill. Requires the swarm-mcp bridge in this repository and the `swarmcloud` MCP server from .mcp.json."
    }
  ]
}
